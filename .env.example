# ── Server-side (API keys — never exposed to browser) ──
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.0-flash
CRM_WEBHOOK_URL=
CRM_API_KEY=

# ── Client-side (VITE_ prefix — bundled in frontend) ──
VITE_CONTACT_WEBHOOK_URL=

# Analytics
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=
